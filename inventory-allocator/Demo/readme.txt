This Inventory Allocation program can be verified by running the .jar file. 

Steps to execute Jar File:

1) Download the .jar file 
2) Open Command Prompt and change the path to the directory where the .jat file is downloaded
3) Enter the command "java -jar InventoryAllocationProgram.jar" to execute the program.
